{"title": "Git - An Excellent File Transferer", "date": "2019-06-07 22:08", "modified": "2019-07-31 18:28", "author": "Sim", "tags": [["git", "git"], ["file", "file"], ["ssh", "ssh"]], "summary": "I recently found that git is an excellent file transferer.", "category_slug": "terminal", "slug": "git-an-excellent-file-transferer", "html": "<p>I did migration of my site from firebase to my server. And then I found out that git is far more better than scp. Normally scp transfer files one by one to the server, while git glues all files together, speeding up the process. Afterwards, the version control will take care of the changes of the whole git project and only process the changes to the server.  </p>\n<p>I did through the official guide 4.3<sup id=\"fnref:1\"><a class=\"footnote-ref\" href=\"#fn:1\">1</a></sup> and 4.4<sup id=\"fnref:2\"><a class=\"footnote-ref\" href=\"#fn:2\">2</a></sup> and created a user <code>git</code> for doing git.  </p>\n<p>However doing without a git account is also possible.  </p>\n<h2 id=\"working-with-a-remote\">Working with a remote</h2>\n<p>Generate my own SSH public key on my local computer:</p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>$ ssh-keygen\n</pre></div>\n</td></tr></table>\n\n<p>Upload my ~/.ssh/id_rsa.pub to somewhere on the server:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@localhost:~$ scp -P port ~/.ssh/id_rsa.pub sim@server:/home/sim/id_rsa.sim.pub\n</pre></div>\n</td></tr></table>\n\n<p>On the server in the home, make dir for ssh if <code>.ssh</code> hasn't been created:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server:~$ mkdir .ssh\n</pre></div>\n</td></tr></table>\n\n<p>append the key to <code>authorized_keys</code>:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server:~$ cat id_rsa.sim.pub &gt;&gt; .ssh/authorized_keys\n</pre></div>\n</td></tr></table>\n\n<p>Initialize a new dir for new git project:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server:~$ git init --bare new_dir.git\n</pre></div>\n</td></tr></table>\n\n<p>On my local computer:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1\n2</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@localhost:~$ <span class=\"nb\">cd</span> project\nsim@localhost:~/project$ git init <span class=\"o\">&amp;&amp;</span> git add . <span class=\"o\">&amp;&amp;</span> git commit -m <span class=\"s2\">&quot;initial commit&quot;</span> <span class=\"o\">&amp;&amp;</span> git remote add origin ssh://sim@server:port:/home/sim/new_dir.git <span class=\"o\">&amp;&amp;</span> git push origin master\n</pre></div>\n</td></tr></table>\n\n<p>Then on the server, I can do this:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server:~$ git clone ~/new_dir.git ~/new_dir\n</pre></div>\n</td></tr></table>\n\n<p><code>new_dir</code> is a new dir with all files checked out from the git project <code>new_dir.git</code> and the git settings.  </p>\n<p>I could create <code>~/new_dir.git/hooks/post-receive</code>:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1\n2\n3\n4</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span><span class=\"ch\">#!/bin/sh</span>\n<span class=\"nb\">cd</span> /home/sim/new_dir <span class=\"o\">||</span> <span class=\"nb\">exit</span>\n<span class=\"nb\">unset</span> GIT_DIR\ngit pull origin master\n</pre></div>\n</td></tr></table>\n\n<p>The <code>unset</code> is needed 'cause git uses the variable <code>GIT_DIR</code> instead of <code>PWD</code>, <code>cd</code> changes <code>PWD</code> instead of <code>GIT_DIR</code>. There must be a fallback from <code>GIT_DIR</code> to <code>PWD</code><sup id=\"fnref:3\"><a class=\"footnote-ref\" href=\"#fn:3\">3</a></sup>.</p>\n<p>Make it executable:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server:~$ chmod +x ~/new_dir.git/hooks/post-receive\n</pre></div>\n</td></tr></table>\n\n<p>Everytime I need update files on the server, I could do it via a bash:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1\n2\n3\n4\n5\n6\n7</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span><span class=\"ch\">#!/bin/bash</span>\n<span class=\"nb\">cd</span> /home/sim/project\n<span class=\"nb\">echo</span> -n <span class=\"s2\">&quot;Enter the commit message: &quot;</span>\n<span class=\"nb\">read</span> message\ngit add .\ngit commit -m <span class=\"s2\">&quot;</span><span class=\"si\">${</span><span class=\"nv\">message</span><span class=\"si\">}</span><span class=\"s2\">&quot;</span>\ngit push origin master\n</pre></div>\n</td></tr></table>\n\n<p>Then everytime I push to the git project, the <code>new_dir</code> will be also updated with new contents.</p>\n<h2 id=\"working-with-multiple-remotes\">Working with multiple remotes</h2>\n<p>Sometimes I need to work with serveral remotes, for example, I need to upload to multiple servers, then I have to do some extra things.  </p>\n<p>Upload my ~/.ssh/id_rsa.pub to somewhere on another server:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@localhost:~$ scp -P port ~/.ssh/id_rsa.pub sim@server2:/home/sim/id_rsa.sim.pub\n</pre></div>\n</td></tr></table>\n\n<p>On the server in the home, make dir for ssh if <code>.ssh</code> hasn't been created:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server2:~$ mkdir .ssh\n</pre></div>\n</td></tr></table>\n\n<p>Append the key to <code>authorized_keys</code>:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server2:~$ cat id_rsa.sim.pub &gt;&gt; .ssh/authorized_keys\n</pre></div>\n</td></tr></table>\n\n<p>Initialize a new dir for new git project:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server2:~$ git init --bare new_dir.git\n</pre></div>\n</td></tr></table>\n\n<p>On my local computer:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1\n2\n3</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@localhost:~$ <span class=\"nb\">cd</span> project\nsim@localhost:~/project$ git remote add origin2 ssh://sim@server2:port:/home/sim/new_dir.git\nsim@localhost:~/project$ git push origin2 master\n</pre></div>\n</td></tr></table>\n\n<p>Then on the server, I can do this:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server2:~$ git clone ~/new_dir.git ~/new_dir\n</pre></div>\n</td></tr></table>\n\n<p><code>new_dir</code> is a new dir with all files checked out from the git project <code>new_dir.git</code> and the git settings.  </p>\n<p>I could create <code>~/new_dir.git/hooks/post-receive</code>:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1\n2\n3\n4\n5</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span><span class=\"ch\">#!/bin/sh</span>\n<span class=\"nb\">cd</span> /home/sim/new_dir <span class=\"o\">||</span> <span class=\"nb\">exit</span>\n<span class=\"nb\">unset</span> GIT_DIR\ngit stash\ngit pull origin master\n</pre></div>\n</td></tr></table>\n\n<p>Make it executable:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span>sim@server2:~$ chmod +x ~/new_dir.git/hooks/post-receive\n</pre></div>\n</td></tr></table>\n\n<p>Everytime I need update files on both servers, I could do it via a bash:  </p>\n<table class=\"codehilitetable\"><tr><td class=\"linenos\"><div class=\"linenodiv\"><pre>1\n2\n3\n4\n5\n6\n7\n8</pre></div></td><td class=\"code\"><div class=\"codehilite\"><pre><span></span><span class=\"ch\">#!/bin/bash</span>\n<span class=\"nb\">cd</span> /home/sim/project\n<span class=\"nb\">echo</span> -n <span class=\"s2\">&quot;Enter the commit message: &quot;</span>\n<span class=\"nb\">read</span> message\ngit add .\ngit commit -m <span class=\"s2\">&quot;</span><span class=\"si\">${</span><span class=\"nv\">message</span><span class=\"si\">}</span><span class=\"s2\">&quot;</span>\ngit push origin master\ngit push origin2 master\n</pre></div>\n</td></tr></table>\n\n<p>Then everytime I push to both git projects, the <code>new_dir</code> on both servers will be also updated with new contents.  </p>\n<div class=\"footnote\">\n<hr />\n<ol>\n<li id=\"fn:1\">\n<p><a href=\"https://git-scm.com/book/en/v2/Git-on-the-Server-Generating-Your-SSH-Public-Key\">Git - Generating Your SSH Public Key</a>&#160;<a class=\"footnote-backref\" href=\"#fnref:1\" title=\"Jump back to footnote 1 in the text\">&#8617;</a></p>\n</li>\n<li id=\"fn:2\">\n<p><a href=\"https://git-scm.com/book/en/v2/Git-on-the-Server-Setting-Up-the-Server\">Git - Setting Up the Server</a>&#160;<a class=\"footnote-backref\" href=\"#fnref:2\" title=\"Jump back to footnote 2 in the text\">&#8617;</a></p>\n</li>\n<li id=\"fn:3\">\n<p><a href=\"https://stackoverflow.com/a/4100577/9850945\">githooks - getting \"fatal: not a git repository: '.'\" when using post-update hook to execute 'git pull' on another repo - Stack Overflow</a>&#160;<a class=\"footnote-backref\" href=\"#fnref:3\" title=\"Jump back to footnote 3 in the text\">&#8617;</a></p>\n</li>\n</ol>\n</div>", "category": "Terminal"}